version: "3"

services:
  mongodb:
    container_name: mongo_server
    image: "mongo:latest"
    restart: always
    networks:
      localnet:
        ipv4_address: 192.168.10.125
    ports:
      - 27017:27017
    volumes:
      - ./volumes/mongodb:/data/db
